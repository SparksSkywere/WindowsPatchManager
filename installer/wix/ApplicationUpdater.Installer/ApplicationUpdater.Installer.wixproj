<Project Sdk="WixToolset.Sdk/5.0.2">

  <PropertyGroup>
    <OutputType>Package</OutputType>
    <OutputName>WindowsPatchManager</OutputName>
    <InstallerPlatform>x64</InstallerPlatform>
    <Platform>x64</Platform>
    <!--
      ICE43/ICE57 flag non-advertised Start Menu / Desktop shortcuts on per-machine packages.
      Suppressing these is standard for machine-wide installs with user-visible shortcuts.
    -->
    <!-- ICE61 warns when AllowSameVersionUpgrades lets Max version == current (intentional). -->
    <SuppressIces>ICE43;ICE57;ICE61</SuppressIces>
    <PayloadDir Condition="'$(PayloadDir)' == ''">$(MSBuildThisFileDirectory)..\..\..\artifacts\payload</PayloadDir>
  </PropertyGroup>

  <ItemGroup>
    <BindPath Include="$(PayloadDir)" BindName="Payload" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="WixToolset.UI.wixext" Version="5.0.2" />
    <PackageReference Include="WixToolset.Util.wixext" Version="5.0.2" />
  </ItemGroup>

</Project>
